For each scenario simply change the value in the :


  col.elevatorList[0].currentFloor = 6
  col.elevatorList[1].currentFloor= 2
  elevator = col.requestElevator(3, "up" )
  elevator.requestFloor(7)

To operate a new algorithm.